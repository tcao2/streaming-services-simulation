class DemoGroup {
  constructor(shortName, longName, numOfAccounts) {
    this.shortName = shortName;
    this.longName = longName;
    this.numOfAccounts = numOfAccounts;
  }
}

export default DemoGroup;
